# src/afd.py
from typing import Dict, Any

def construir_afd(tabla_transicion: Dict[Any, Any], primera_pos: set) -> Dict[Any, Any]:
    """
    Construye el AFD a partir de la tabla de transición y la primera posición obtenida del árbol sintáctico.
    
    Parámetros:
        tabla_transicion (Dict): Tabla de transición generada.
        primera_pos (set): Conjunto de posiciones de la raíz del árbol.
    
    Retorna:
        Dict: Estructura del AFD, que incluye estados, alfabeto, función de transición, estado inicial y estados de aceptación.
    """
    pass
